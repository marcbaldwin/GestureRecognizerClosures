language: objective-c
osx_image: xcode7.2

before_install:
  - gem install cocoapods --no-rdoc --no-ri --no-document --quiet
  - gem install xcpretty --no-rdoc --no-ri --no-document --quiet

script:
  - set -o pipefail
  - xcodebuild test -workspace GestureRecognizerClosures.xcworkspace -scheme GestureRecognizerClosures -configuration Debug -destination "platform=iOS Simulator,name=iPhone 5,OS=8.1" -destination "platform=iOS Simulator,name=iPhone 6,OS=9.0" ONLY_ACTIVE_ARCH=NO ENABLE_TESTABILITY=YES | xcpretty -c
